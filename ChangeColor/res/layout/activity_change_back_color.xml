<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/lyBase"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".ChangeBackColor" >

    <TextView
        android:id="@+id/tvColore"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/tvColore" />

    <EditText
        android:id="@+id/etColor"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/tvColore"
        android:layout_below="@+id/tvColore"
        android:layout_marginTop="22dp"
        android:ems="10"
        android:hint="@string/etColor" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/btOK"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/etColor"
        android:layout_below="@+id/etColor"
        android:layout_marginRight="15dp"
        android:layout_marginTop="31dp"
        android:text="@string/btOK" />

</RelativeLayout>